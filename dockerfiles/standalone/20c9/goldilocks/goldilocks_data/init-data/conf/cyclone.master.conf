##################################
#        Cyclone Configure       #
#             MASTER             #
##################################

# PROTOCOL          = DA                     # DA or TCP
# HOST_IP           = 127.0.0.1
# HOST_PORT         = 22581
# DSN               = GOLDILOCKS

# USER_ID           = testId
# USER_PW           = testPw
# USER_ENCRYPT_PW   = 't33KImiqvhqNyfN+uZmFrw=='
# LOG_CAPTURE_INTERVAL_1 = 200               # Unit : Millisecond (default:200 ms)
# LOG_CAPTURE_INTERVAL_2 = 1000              # Unit : Millisecond (default:1000 ms)

GROUP_NAME = Group1
{
    # LOG_PATH='/home/LogMirrorWal'          # Log Path with LogMirror
    # TRANS_SORT_AREA_SIZE = 500             # Transaction Sort Area Size For Cyclone Capture (default : 500M)
    # TRANS_FILE_PATH='/tmp/'                # Temporary Transaction File Path    
    # READ_LOG_BLOCK_COUNT = 20480           # Read Block Count ( X * 512 , default : 10M ) 
    # COMM_CHUNK_COUNT     = 32              # Communication Buffer size ( 1 Chunk = 16M, default : 512M)
    # PORT = 21102                           # Cyclone Port
    # GIVEUP_INTERVAL      = 0               # Capture Interval Limit (default: disable(0))
    # SYNCHER_COUNT        = 4               # Syncher Count (default : 4, Max : 20)
    # SYNC_ARRAY_SIZE      = 1000            # Sync Array Size (default : 1000)
    # TCP_NODELAY = 1                        # Tcp Nodelay mode (1:Enable, 0: Disable, default : enable)
    # HEARTBEAT_TIMEOUT    = 30              # Heartbeat timeout second (default : 30, min : 10)
    # PACKET_COMPRESSION_MODE = 1            # Packet Compression Mode(1:Enable, 0:Disable, Default : Enable)
    
    CAPTURE_TABLE =                          # Capture Table List
    (
        testTable1,
        testTable2
    )
}

GROUP_NAME = Group2
{
    # PORT = 21103           # Cyclone Port
    
    CAPTURE_TABLE =          # Capture Table List
    (
        testTable3,
        testTable4
    )
}
